---
permalink: 404.html
layout: layouts/base.njk
title: 404 - Not Found
---

<style>
.not-found-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-xxl) var(--spacing-md);
    text-align: center;
    min-height: 60vh;
}

.radar-wrapper {
    position: relative;
    margin-bottom: var(--spacing-xl);
}

.radar {
    width: 240px;
    height: 240px;
    border-radius: 50%;
    background: #0f172a;
    position: relative;
    overflow: hidden;
    border: 4px solid var(--color-primary);
    box-shadow: 0 0 30px rgba(43, 95, 167, 0.3), inset 0 0 20px rgba(43, 95, 167, 0.5);
}

/* Grid lines for the radar */
.radar::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    height: 1px;
    background: rgba(43, 95, 167, 0.4);
}

.radar::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 1px;
    background: rgba(43, 95, 167, 0.4);
}

/* Radar rings */
.radar-ring {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    border: 1px solid rgba(43, 95, 167, 0.3);
}

.radar-ring:nth-child(1) { width: 60px; height: 60px; }
.radar-ring:nth-child(2) { width: 120px; height: 120px; }
.radar-ring:nth-child(3) { width: 180px; height: 180px; }

/* The sweeping beam */
.sweep {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 120px;
    height: 120px;
    background: conic-gradient(from 0deg at 0 0, rgba(43, 95, 167, 0) 0deg, rgba(43, 95, 167, 0.6) 90deg, rgba(43, 95, 167, 0) 90deg);
    transform-origin: 0 0;
    animation: sweep 4s linear infinite;
}

@keyframes sweep {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

/* Random blips that appear and disappear */
.blip {
    position: absolute;
    width: 8px;
    height: 8px;
    background-color: #ef4444; /* red blip indicating an error/404 */
    border-radius: 50%;
    box-shadow: 0 0 10px #ef4444;
    opacity: 0;
    animation: blip 4s infinite;
}

.blip-1 { top: 30%; left: 60%; animation-delay: 0.5s; }
.blip-2 { top: 70%; left: 40%; animation-delay: 2.1s; }
.blip-3 { top: 45%; left: 20%; animation-delay: 3.5s; }

@keyframes blip {
    0%, 10% { opacity: 0; transform: scale(0.5); }
    15% { opacity: 1; transform: scale(1.5); }
    20%, 100% { opacity: 0; transform: scale(0.5); }
}

.not-found-content h1 {
    font-size: 5rem;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
    line-height: 1;
}

.not-found-content h2 {
    font-size: var(--fs-xl);
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
}

.not-found-content p {
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto var(--spacing-lg);
    line-height: 1.6;
}

.return-button {
    display: inline-block;
    padding: 12px 32px;
    background-color: var(--color-primary);
    color: white;
    text-decoration: none;
    border-radius: var(--radius-full);
    font-weight: 600;
    transition: all 0.3s ease;
    box-shadow: var(--shadow-md);
}

.return-button:hover {
    background-color: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    color: white;
}
</style>

<div class="not-found-container container">
    <div class="radar-wrapper">
        <div class="radar">
            <div class="radar-ring"></div>
            <div class="radar-ring"></div>
            <div class="radar-ring"></div>
            <div class="blip blip-1"></div>
            <div class="blip blip-2"></div>
            <div class="blip blip-3"></div>
            <div class="sweep"></div>
        </div>
    </div>
    
    <div class="not-found-content">
        <h1 id="error-code">404</h1>
        <h2 id="error-title">Signal Lost</h2>
        <p id="error-desc" style="margin-bottom: 2rem;">The page you are looking for has vanished from our radar. It might have been moved, deleted, or blown away by strong winds.</p>
        
        <a href="/en/" id="error-btn" class="return-button">Return to Base Station</a>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        const path = window.location.pathname;
        const titleEl = document.getElementById("error-title");
        const descEl = document.getElementById("error-desc");
        const btnEl = document.getElementById("error-btn");

        if (path.startsWith('/id/')) {
            document.title = "404 - Tidak Ditemukan";
            titleEl.textContent = "Sinyal Hilang";
            descEl.textContent = "Halaman yang Anda cari telah hilang dari radar kami. Mungkin telah dipindahkan, dihapus, atau tertiup angin kencang.";
            btnEl.textContent = "Kembali ke Beranda";
            btnEl.href = "/id/";
        } else if (path.startsWith('/fr/')) {
            document.title = "404 - Introuvable";
            titleEl.textContent = "Signal Perdu";
            descEl.textContent = "La page que vous recherchez a disparu de notre radar. Elle a peut-être été déplacée, supprimée ou emportée par des vents violents.";
            btnEl.textContent = "Retour à la Station de Base";
            btnEl.href = "/fr/";
        }
    });
</script>
